# v0.3 Chrome QA

Date: 2026-06-08
Branch: `feat/v0.3-accounts-ux-hosts`
Local URL: `http://localhost:5173`

## Command Verification

- PASS: production slash passwordless route returned `200 {"sent":true}` for
  `POST https://winworldcup2026.com/api/auth/passwordless/start`.
- PASS: `npm run test:v0.1`
- PASS: `npm run test:v0.2`
- PASS: `npm run test:v0.3`
- PASS: `npm run lint`
- PASS: `npm run build`
- PASS: `npx vercel build`
- PASS: `npm run db:apply`

## Chrome Visual Checks

- PASS: English/dark `/pickem` quick-fill rendered the desktop two-sided
  bracket with 31 knockout matches, eight side rounds, visible left/right
  halves, and a central Final/Champion column.
  Evidence: `tests/e2e/screenshots/v0.3/pickem-desktop-bracket-en-dark.png`
- PASS: after fixing the `.pk-sec` margin cascade, the Pick'em nav, hero, and
  all `.wrap-wide.pk-sec` sections share the same measured `left=320`,
  `right=1600`, and `width=1280` desktop alignment.
  Evidence: `tests/e2e/screenshots/v0.3/pickem-aligned-en-dark.png`
- PASS: English/light header retained the shared 1280px content width and a
  148px logo footprint.
  Evidence: `tests/e2e/screenshots/v0.3/header-en-light.png`
- PASS: Arabic/light header used `dir="rtl"` for the page while the SVG
  wordmark and `WIN·2026` text computed as LTR; the text bounding box stayed
  inside the SVG bounding box.
  Evidence: `tests/e2e/screenshots/v0.3/header-ar-light.png`
- PASS: Arabic/dark header retained the 148px non-shrinking logo footprint and
  1280px content width; the wordmark text stayed inside the SVG bounding box.
  Evidence: `tests/e2e/screenshots/v0.3/header-ar-dark.png`

## Homepage Lock Gate

- PASS: clicking the homepage `Lock my prediction` button opened the
  website-styled email-code gate.
- PASS: the gate rendered `Sign in to lock it`, showed an email input, and did
  not render a password field.
- BLOCKED: before the email could be submitted, Chrome automation detached
  because another extension UI was open on the page. Retrying on a clean
  `localhost:5174` origin hit the same extension UI block before the email could
  be sent. No fresh email code was sent during these attempts.

## Hosts API Smoke

Because Chrome was blocked before an authenticated session could be established,
the database-backed hosts path was also exercised through the local API with a
disposable signed QA session and then cleaned up.

- PASS: created two hosts for one QA user.
- PASS: joined one host by code and one by slug/link payload.
- PASS: public host responses returned one handle-only member on each host.
- PASS: the same handle appeared in both hosts, proving multi-host membership.
- PASS: serialized public host responses did not include email, address, Auth0
  identifiers, or user IDs.
- PASS: the disposable hosts, memberships, and QA user were deleted after the
  smoke check.

## Remaining Browser Items

- PENDING: complete the authenticated Chrome browser path after the blocking
  extension UI is dismissed: send the Auth0 email code, verify the code, persist
  the homepage prediction after signup, create a host from `/hosts`, join by
  `/h/:slug?join=CODE`, join by typed code, verify multi-host state, and inspect
  the public `/h/:slug` page in English and Arabic across dark/light themes.
