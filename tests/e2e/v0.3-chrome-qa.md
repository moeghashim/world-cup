# v0.3 Chrome QA

Date: 2026-06-08
Branch: `feat/v0.3-accounts-ux-hosts`
Local URL: `http://localhost:5173`

## Command Verification

- PASS: production slash passwordless route returned `200 {"sent":true}` for
  `POST https://winworldcup2026.com/api/auth/passwordless/start`.
- PASS: `npm run test:v0.1`
- PASS: `npm run test:v0.2`
- PASS: `npm run test:v0.3`
- PASS: `npm run lint`
- PASS: `npm run build`
- PASS: `npx vercel build`
- PASS: `npm run db:apply`

## Chrome Visual Checks

- PASS: English/dark `/pickem` quick-fill rendered the desktop two-sided
  bracket with 31 knockout matches, eight side rounds, visible left/right
  halves, and a central Final/Champion column.
  Evidence: `tests/e2e/screenshots/v0.3/pickem-desktop-bracket-en-dark.png`
- PASS: after fixing the `.pk-sec` margin cascade, the Pick'em nav, hero, and
  all `.wrap-wide.pk-sec` sections share the same measured `left=320`,
  `right=1600`, and `width=1280` desktop alignment.
  Evidence: `tests/e2e/screenshots/v0.3/pickem-aligned-en-dark.png`
- PASS: English/light header retained the shared 1280px content width and a
  148px logo footprint.
  Evidence: `tests/e2e/screenshots/v0.3/header-en-light.png`
- PASS: Arabic/light header used `dir="rtl"` for the page while the SVG
  wordmark and `WIN·2026` text computed as LTR; the text bounding box stayed
  inside the SVG bounding box.
  Evidence: `tests/e2e/screenshots/v0.3/header-ar-light.png`
- PASS: Arabic/dark header retained the 148px non-shrinking logo footprint and
  1280px content width; the wordmark text stayed inside the SVG bounding box.
  Evidence: `tests/e2e/screenshots/v0.3/header-ar-dark.png`

## Homepage Lock Gate

- PASS: clicking the homepage `Lock my prediction` button opened the
  website-styled email-code gate.
- PASS: the gate rendered `Sign in to lock it`, showed an email input, and did
  not render a password field.
- PASS: in a clean extension-free Chrome profile, the same website-styled gate
  submitted `moe@babanuj.com`, Auth0 returned the code-entry step, and the page
  displayed `Check your inbox for the one-time code`.
- REVIEW ON DEPLOYED LINK: relayed local OTPs were rejected by Auth0 as
  `invalid_code`, even though the modal reached code-entry and Auth0 delivered
  codes. The user requested that Claude review this signup step against the
  deployed/preview link instead of continuing the local OTP path.

## Authenticated Browser QA

Because relayed OTPs were rejected locally and the user moved the Auth0
email-code check to deployed-link review, the protected app flows were completed
in the clean Chrome profile with a disposable signed QA session and handle. The
disposable database rows were deleted after evidence capture.

- PASS: `/profile` rendered as authenticated with the QA handle, account email,
  signup country, and `Sign out`.
- PASS: the homepage prediction was already migrated/persisted for Match 1, did
  not show the sign-in gate, and remained `✓ Locked in — good luck!` after a
  full page reload.
  Evidence: `tests/e2e/screenshots/v0.3/home-authenticated-lock-en-dark.png`
- PASS: `/hosts` created a host from the desktop UI, returned a unique
  `/h/:slug?join=CODE` link, rendered a QR code, and displayed the short join
  code.
  Evidence: `tests/e2e/screenshots/v0.3/hosts-create-en-dark.png`
- PASS: `/hosts` joined a second host by typed code from the desktop UI.
  Evidence: `tests/e2e/screenshots/v0.3/hosts-join-code-en-dark.png`
- PASS: the public `/h/:slug?join=CODE` page auto-joined the current handle,
  showed two handle-only members, showed member count, QR code, join code, and
  consensus placeholder data, and did not render emails or private IDs.
  Evidence: `tests/e2e/screenshots/v0.3/host-public-joined-en-dark.png`
- PASS: the host created by the current QA user rendered on public
  `/h/:slug?join=CODE` with the owner handle, QR code, join code, and no email
  or private IDs.
  Evidence: `tests/e2e/screenshots/v0.3/host-public-owner-en-dark.png`
- PASS: Arabic/light host public page rendered with `lang="ar"`, `dir="rtl"`,
  Arabic navigation, a fixed 148px logo footprint, and no horizontal overflow.
  Evidence: `tests/e2e/screenshots/v0.3/host-public-ar-light.png`
- PASS: Arabic/dark host public page rendered with `lang="ar"`, `dir="rtl"`,
  Arabic navigation, a fixed 148px logo footprint, and no horizontal overflow.
  Evidence: `tests/e2e/screenshots/v0.3/host-public-ar-dark.png`
- PASS: Neon cleanup deleted the disposable `codex-v03-*@example.test` users and
  cascade-owned host data after screenshots were captured.
