# v0.4 Chrome QA - Live Results + Scoring

Date: 2026-06-08

Local target: `http://localhost:5173`

Browser harness: Playwright Chromium with same-origin API mocks for visible QA
state. The server/scoring correctness is covered by `npm run test:v0.4` and
`npm run db:apply`; the browser pass avoids inserting fake match results into
the shared Neon database before the tournament starts.

## Evidence

Screenshots saved under `tests/e2e/screenshots/v0.4/`:

- `host-points-en-dark.png`
- `profile-points-en-dark.png`
- `host-points-ar-light.png`
- `profile-points-ar-light.png`

## Checks

- Host page renders a leaderboard member with `330` points.
- Profile page renders the signed-in user's `330` points.
- Football-data attribution is visible when the active source is
  `football-data`.
- English dark and Arabic light screenshots captured.
- Spot-checked English, Spanish, French, Portuguese, and Arabic in both dark and
  light themes.
- Arabic sets `dir="rtl"` and non-Arabic languages set `dir="ltr"`.
- No horizontal overflow at 1365px desktop viewport.
- No browser console errors or page errors were observed.

## Boundary

All real data-provider calls remain server-side. The browser only saw same-origin
responses from `/api/standings`, `/api/hosts/:slug`, and `/api/auth/me`.
