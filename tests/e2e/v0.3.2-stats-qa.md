# v0.3.2 Real Stats QA

Date: 2026-06-08

## Local Setup

- Branch: `feat/v0.3.2-real-stats`
- Local app: `http://localhost:5173`
- Dev command: `npm run dev`
- Community API: `GET /api/data/community`

## Live Local Aggregate Snapshot

The local API used Neon-backed data during screenshot QA:

```json
{
  "players": 2,
  "bracketsLocked": 1,
  "hostsJoined": 0,
  "championDistribution": [],
  "communityBrackets": [],
  "source": {
    "fallback": false
  }
}
```

Notes:

- `players` is real and comes from users with at least one locked bracket, group pick, or match prediction.
- `bracketsLocked` is real and replaces the old fabricated bracket-views tile.
- Champion distribution is empty because the one locked bracket in the current data does not include a champion pick yet.
- R32 consensus has one real pick: MEX at 100% for R32 match 1.
- Community comparison samples are empty because no locked bracket currently has both a public handle and a champion.

## Browser Evidence

All screenshots were captured with Playwright against the local app. Every page reported `GET /api/data/community` as `200`.

| Page | Language | Direction | Screenshot | Evidence |
| --- | --- | --- | --- | --- |
| Home | English | LTR | `tests/e2e/screenshots/v0.3.2/home-en-dark.png` | Hero trust shows `2+ players`, prize joined shows `2`, champion card uses empty-state copy. |
| Home | Arabic | RTL | `tests/e2e/screenshots/v0.3.2/home-ar-dark.png` | RTL layout renders the same real player count and Arabic empty-state copy. |
| Brackets | English | LTR | `tests/e2e/screenshots/v0.3.2/brackets-en-dark.png` | Ticker/stat strip shows `1` locked bracket, top champion share `0%`, host members `0`, R32 M1 shows MEX 100%, and public samples show empty-state copy. |
| Brackets | Arabic | RTL | `tests/e2e/screenshots/v0.3.2/brackets-ar-dark.png` | RTL layout renders the same locked-bracket and R32 consensus data with Arabic labels. |
| Sponsors | English | LTR | `tests/e2e/screenshots/v0.3.2/sponsors-en-dark.png` | Reach tiles show `2` active players and `1` brackets locked; match/day counts remain tournament config. |
| Sponsors | Arabic | RTL | `tests/e2e/screenshots/v0.3.2/sponsors-ar-dark.png` | RTL sponsor layout renders the same real reach stats with Arabic labels. |

## Checks

- PASS: No fabricated `players`, `views`, joined, champion-distribution, or community-bracket sample numbers remain in active app source.
- PASS: Low-data state renders without `NaN` or divide-by-zero.
- PASS: API response is handle-only and does not include email, Auth0 IDs, addresses, or raw user IDs.
- PASS: No DB migration was added.
- PASS: No points, ranks, standings, scores, or host leaderboard scoring were added.
