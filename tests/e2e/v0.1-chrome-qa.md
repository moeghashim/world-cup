# v0.1 Chrome QA

Status: Ready for review with one browser-environment limitation. Auth0
email-code verification, first-sign-in handle setup, authenticated persistence,
and profile API checks passed with a human-supplied code from `moe@babanuj.com`.
Extension-free browser visual coverage passed for English, Arabic RTL, dark
theme, and light theme. Chrome-owned session-cookie completion from the modal is
still blocked by another Chrome extension UI, not by the app endpoints.

## 2026-06-08 Auth0 Follow-Up And Visual QA

- User approved sending Auth0 test codes to `moe@babanuj.com`.
- The app modal sent a fresh code to `moe@babanuj.com`, but a stale/older code
  entered in the Chrome modal was rejected with the expected localized
  `That code did not work.` message.
- A fresh follow-up code from the same inbox was accepted by
  `POST /api/auth/passwordless/verify` through the local Vite/API dev path.
  The endpoint returned `200`, set the signed httpOnly app session, and returned
  the first-sign-in redirect
  `/profile?setup=handle&returnTo=%2Fpickem%23group`.
- `/api/auth/me` with the accepted session confirmed
  `authenticated: true`, `needsHandle: true`, email `moe@babanuj.com`, and no
  handle before first-sign-in setup.
- First-sign-in handle setup passed by setting handle `moe2026`; the response
  confirmed `needsHandle: false`.
- Authenticated persistence passed through production app API routes:
  - group picks saved/reloaded with `locked: true` and 3 picks
  - score prediction `match-qa-1` saved/reloaded with `2-1` and `locked: true`
  - bracket saved/reloaded with `locked: true`, 2 groups, and final pick `BRA`
  - `/api/profile` returned email `moe@babanuj.com`, handle `moe2026`, and
    `needsHandle: false`
- Chrome visual run evidence:
  - `/pickem` loaded in Chrome, quick-fill favorites populated the bracket, and
    the website-styled Auth0 email-code modal opened from `Lock my bracket`.
  - Chrome extension state blocked structured automation while the modal was
    active. Pressing Escape dismissed the visible modal overlay, but Chrome
    automation still reported another extension UI blocking the page.
- Extension-free in-app browser visual evidence:
  - English dark: `/pickem`, `dir="ltr"`, `data-theme="dark"`,
    `h1="Build your bracket"`, no console errors.
  - English light: `dir="ltr"`, `data-theme="light"`,
    `h1="Build your bracket"`, no console errors.
  - Arabic light: `lang="ar"`, `dir="rtl"`, `data-theme="light"`,
    `h1="كوّن جدولك"`, no console errors.
  - Arabic dark: `lang="ar"`, `dir="rtl"`, `data-theme="dark"`,
    `h1="كوّن جدولك"`, no console errors.

Residual risk for review:

- The browser-owned Chrome session cookie was not completed from the modal
  because Chrome extension UI kept blocking automation after code entry. The
  same app endpoint did accept the live Auth0 code and set the httpOnly session
  in the local API path.
- The authenticated `/profile` page was verified at the API contract level, not
  as an authenticated Chrome-rendered page, because the accepted session cookie
  was owned by the local API QA request rather than Chrome.

## 2026-06-08 OTP Session And Persistence Check

- User supplied a fresh Auth0 code for `moe@babanuj.com`.
- `POST /api/auth/passwordless/verify` through the local Vite/API dev path
  returned an authenticated session and redirect
  `/profile?setup=handle&returnTo=%2Fpickem%23group`.
- `/api/auth/me` with the returned cookie confirmed
  `authenticated: true`, `needsHandle: true`, and user email
  `moe@babanuj.com`.
- Follow-on authenticated API QA used the same signed app-session format for
  the verified Auth0 user and only proceeded after confirming the account had no
  existing persisted group picks, predictions, or bracket rows.
- First-sign-in handle setup passed by setting handle `moe2026`.
- Authenticated persistence passed:
  - group picks saved and reloaded with two picks
  - one score prediction saved and reloaded
  - one locked bracket saved and reloaded

Remaining browser-specific checks:

- Browser-owned session cookie from the email-code modal.
- Anonymous-pick migration after Auth0 verification.
- `/profile` rendered with an authenticated Auth0-backed browser session.
- Arabic RTL and both-theme coverage of the authenticated browser flow.

## 2026-06-08 Auth0 Email Delivery Follow-Up

- Auth0 Passwordless Email connection `email` is enabled and
  `/api/auth/passwordless/start` now returns `200` with `{ "sent": true }`.
- Auth0 custom SMTP through AgentMail failed for both
  `worldcup-auth-qa-202606080349@agentmail.to` and `moe@babanuj.com`.
  Auth0 recorded `Code/Link Sent` followed immediately by
  `Failed Sending Notification` with
  `Error sending email: Mail command failed: 550 550 5.1.8 Sender address rejected`.
- Provider-level From and the `Verification Email (Code)` template From were both
  set to `world-cup-agent@agentmail.to`; the SMTP failure persisted.
- Direct AgentMail SMTP using the same inbox address and API key queued
  successfully to `moe@babanuj.com`, and the user confirmed receiving the direct
  SMTP test. The issue is therefore isolated to Auth0 custom SMTP handoff.
- Auth0 custom SMTP was disabled. With Auth0 built-in delivery, Auth0 logged a
  clean `Code/Link Sent` row for `moe@babanuj.com` at
  `2026-06-08T13:04:35.917Z` and no new `Failed Sending Notification` row.
- Polling the AgentMail QA inbox for Auth0 built-in delivery did not receive a
  code within the test window, so the sign-off run remains human-assisted via
  `moe@babanuj.com`.

Remaining unverified before the API session check:

- Entering a fresh six-digit Auth0 code and establishing the app session.
- Handle-at-first-sign-in.
- Anonymous-pick migration after Auth0 verification.
- Account-backed picks persisting across reload/fresh session.
- `/profile` with an authenticated Auth0-backed session.
- Arabic RTL and both-theme coverage of the authenticated flow.

## 2026-06-08 Auth0 Smoke

- Projects.dev provisioned an Auth0 client resource named `auth0`.
- The default Projects.dev environment now includes `auth0`; the old WorkOS
  `auth` resource is detached from the active environment.
- Local `/api/auth/start?returnTo=/pickem#group` returns `302`, sets
  `wwc_auth_state` as an httpOnly nonce cookie, and redirects to
  `https://worldcup2026.us.auth0.com/authorize`.
- Auth0 initially returned `unauthorized_client: Callback URL mismatch` for
  `http://localhost:5173/api/auth/callback`.
- `stripe projects update auth0` accepted callback/logout/web-origin config for
  localhost and `https://winworldcup2026.com`.
- After the update, Auth0 `/authorize` redirects to `/u/login`, confirming the
  callback mismatch is fixed.
- Browser smoke check on `/pickem`: quick-fill favorites, click
  `Lock my bracket`, confirm gate copy says `Auth0 handles the secure sign-in`,
  click `Continue with Auth0`, and land on Auth0 Universal Login with title
  `Log in | World Cup`.

Remaining unverified after provider swap:

- Completed Auth0 login with a real user.
- Handle-at-first-sign-in.
- Anonymous-pick migration after Auth0 callback.
- Account-backed picks persisting across reload/fresh session.
- `/profile` with an authenticated Auth0-backed session.
- Arabic RTL and both-theme coverage of the authenticated flow.

## 2026-06-07 Historical WorkOS Blocker

This file records the real-browser QA evidence for the v0.1 accounts and
persistence milestone before the milestone PR is opened.

## Environment

- Date: 2026-06-07
- Browser: Google Chrome through the Codex Chrome skill
- Local app: `http://localhost:5173`
- Local API shim: `http://127.0.0.1:5181`
- Theme/language coverage started with English + dark theme on `/pickem`

## Evidence

### Passed

- `/pickem` loaded in Chrome at `http://localhost:5173/pickem`.
- The page rendered `Build your bracket`, `dir="ltr"`, and
  `data-theme="dark"`.
- Anonymous play path worked: `Quick-fill favourites` populated the bracket and
  enabled `Lock my bracket`.
- Clicking `Lock my bracket` while signed out opened the account gate with
  `Sign in to lock it`, `Sign in with email`, and `Keep editing`.
- The WorkOS handoff reached hosted AuthKit after the localhost redirect URI was
  registered in WorkOS:
  `https://crisp-grace-89-staging.authkit.app/...`.
- AuthKit rendered the `Sign in` screen with one email input and a `Continue`
  button. The previous WorkOS `redirect-uri-invalid` page no longer appears for
  `http://localhost:5173/api/auth/callback`.

### Blocked

- Full WorkOS sign-in/session completion is not verified yet because the hosted
  AuthKit flow requires a one-time email code delivered to an inbox.
- A Chrome extension UI then blocked further browser automation on the AuthKit
  page before the QA email submission could be completed.
- Because the WorkOS callback was not completed, these required checks remain
  unverified in Chrome:
  - handle-at-first-sign-in
  - anonymous-pick migration after sign-in
  - account-backed picks persisting across reload/fresh session
  - `/profile` with an authenticated WorkOS-managed session
  - Arabic RTL and both-theme coverage of the authenticated flow

## Needed To Finish

- Dismiss the blocking Chrome extension UI.
- Provide an accessible QA inbox/code for the WorkOS AuthKit one-time code, or
  approve a deliberately scoped local-only test helper for session seeding that
  is not shipped to production.
